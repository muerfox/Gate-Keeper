{
  "name": "@gatekeeper/shared",
  "version": "0.1.0",
  "description": "Gate Keeper shared types, Zod schemas, and Prisma client.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./prisma": "./generated/client/index.js"
  },
  "scripts": {
    "build": "prisma generate --schema prisma/schema.prisma && tsup src/index.ts --format esm --dts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.6.3",
    "tsup": "^8.3.5"
  }
}
