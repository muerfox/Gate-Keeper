{
  "name": "@gatekeeper/rate-limit",
  "version": "0.1.0",
  "description": "Gate Keeper distributed rate limiting and atomic one-time-token replay protection (Redis-backed, Postgres fallback).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gatekeeper/shared": "0.1.0",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.4",
    "ioredis-mock": "^8.9.0"
  }
}
