{
  "name": "@gatekeeper/crypto",
  "version": "0.1.0",
  "description": "Gate Keeper cryptographic primitives: signed challenges, tokens, password hashing. No invented cryptography — thin wrappers over jose/node:crypto.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.4"
  }
}
