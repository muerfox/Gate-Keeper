{
  "name": "@gatekeeper/offline",
  "version": "0.1.0",
  "description": "Gate Keeper offline/self-hosted mode: local issuance and verification of cryptographic challenges with no network dependency on the hosted API. See docs/OFFLINE_MODE.md for the trust-model limitations before using this in production.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gatekeeper/crypto": "0.1.0",
    "@gatekeeper/challenges": "0.1.0",
    "@gatekeeper/risk-engine": "0.1.0",
    "@gatekeeper/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.4"
  }
}
