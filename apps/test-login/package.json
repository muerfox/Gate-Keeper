{
  "name": "@gatekeeper/test-login",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal login page, guarded by the real Gate Keeper widget and re-verified server-side, used to smoke-test a Gate Keeper deployment end-to-end. See docs/TESTING.md.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch server.ts",
    "seed": "tsx scripts/seed-test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gatekeeper/captcha-server": "0.1.0",
    "@gatekeeper/shared": "0.1.0",
    "@gatekeeper/crypto": "0.1.0",
    "fastify": "^5.1.0",
    "@fastify/static": "^10.1.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.9.0"
  }
}
